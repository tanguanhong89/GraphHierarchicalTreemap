google map style panel
    token filtering
lock mouseover
Minimap depth 3
finetune tooltip domPos, too much positive

Possible failing to find path if fully blocked

Remove g- groups
Pathing optimization, reuse existing paths as much as possible(till closest node to dst, then draw new paths)
Setup proper build scripts